/**
 * Sections optionnelles, non prévues dans le cahier des charges TC BTP
 * mais conservées en option pour renforcer la crédibilité du site.
 * Mettre à `false` pour les masquer sans supprimer le code.
 */
export const featureFlags = {
  showTeamSection: true,
  showTestimonialsSection: true,
  showBlog: true,
};